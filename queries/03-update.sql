UPDATE usuarios SET username = 'ProGamer2025'
WHERE username = 'joao.silva';

SELECT * FROM usuarios;

UPDATE usuarios SET pontos = pontos + 1000;

SELECT * FROM usuarios;

UPDATE usuarios SET email = 'pro@exemplo.com'
WHERE email = 'joao.silva@exemplo.com';

SELECT * FROM usuarios;